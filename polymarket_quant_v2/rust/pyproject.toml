[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "pqv2_accel"
version = "0.1.0"
description = "CPU-bound kernels for the Polymarket Quant Engine V2"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
